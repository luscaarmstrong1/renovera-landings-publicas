# Dados de localidades

`ibge-localities.json` e gerado por `scripts/generate-official-local-data.mjs` a partir da API de localidades do IBGE. O snapshot contem UFs e municipios, e deve ser atualizado executando `node scripts/generate-official-local-data.mjs` antes de uma nova publicacao quando necessario.
